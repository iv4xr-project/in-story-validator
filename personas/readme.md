#Twine Parser Assumptions

In order to simplify the twine parser, we created the following assumptions:
- Variables should be integer numbers.
- Set statements are all executed before checking for possible links.